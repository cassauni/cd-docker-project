server {
    listen 80;
    server_name cd-docker-project.ignorelist.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name cd-docker-project.ignorelist.com;

    ssl_certificate /etc/letsencrypt/live/cd-docker-project.ignorelist.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/cd-docker-project.ignorelist.com/privkey.pem;

    location / {
        proxy_pass http://web:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}