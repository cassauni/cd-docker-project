services:
  web:
    build: .
    volumes:
      - .:/app
    ports:
      - "5001:5000"
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1

  nginx:
    profiles: ["donotstart"]
  certbot:
    profiles: ["donotstart"]